
import javax.swing.*;

public class Calendar extends JFrame {

    public Calendar() {
        setTitle("Calendar");
        setSize(800, 600);
        setDefaultCloseOperation(WindowConstants.DISPOSE_ON_CLOSE);
        setLocationRelativeTo(null);

        // Add your appointments-related components and logic here
    }
}